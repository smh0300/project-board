<?xml version="1.0"?>
<thlogic>


    <attr sel="#login-form" th:action="@{/login}" th:method="post"/>
    <attr sel="#sign-up" th:href="@{/sign-up}"/>
    <attr sel="#loginerror" th:if="@{param.error}" />
    <attr sel="#loginerrortxt" th:text="${exception}" />

    <attr sel="#signUpSuccess" th:if="${success}" th:text="${success}"/>

</thlogic>